apiVersion: v1
kind: Service
metadata:
  name: expense-tracker-service
spec:
  type: ClusterIP
  selector:
    app: expense-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081

